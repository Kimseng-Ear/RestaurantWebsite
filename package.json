{
  "name": "leisure_lake_website",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix client\" \"npm run dev --prefix server\"",
    "seed": "npm run seed --prefix server",
    "start": "node server/server.js",
    "deploy": "npm run build --prefix client && npx firebase-tools deploy --only hosting"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "concurrently": "^9.2.1"
  }
}
